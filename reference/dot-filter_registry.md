# Environment storing registered filter type constructors.

Environment storing registered filter type constructors.

## Usage

``` r
.filter_registry
```

## Format

An object of class `environment` of length 0.
